name: Quality Checks

# Industry best practice: "Fail fast locally, verify comprehensively remotely"
#
# Local (pre-commit): Lint + format (staged files) - instant feedback
# Local (pre-push): Lint + format + secrets + changed dependency audit
# CI (this workflow): Affected tests, Semgrep, and other security checks
#
# The local gate stays fast. CI is the full safety net for this generator repo.

on:
  push: # Kept: catches direct pushes and scheduled scans
    branches: [main, master, develop]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      - '.editorconfig'
  pull_request:
    branches: [main, master, develop]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      - '.editorconfig'
  schedule:
    - cron: '0 0 1 * *' # Monthly on 1st (security scans)
  workflow_dispatch: # Manual trigger

# Prevent duplicate runs - cancel in-progress when new commit pushed
concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Consumer workflows are read-only by default. Jobs that post PR comments
# declare their narrow pull-requests permission explicitly.
permissions:
  contents: read

# WORKFLOW_MODE: minimal

jobs:
  # Step 1: Detect project maturity level and package manager
  # Skip for Dependabot PRs - they auto-merge based on their own checks
  # This reduces GitHub Actions minutes by ~50% on active repos
  detect-maturity:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.actor != 'dependabot[bot]' || github.event_name == 'schedule'
    outputs:
      maturity: ${{ steps.detect.outputs.maturity }}
      source-count: ${{ steps.detect.outputs.source-count }}
      test-count: ${{ steps.detect.outputs.test-count }}
      has-deps: ${{ steps.detect.outputs.has-deps }}
      has-docs: ${{ steps.detect.outputs.has-docs }}
      has-css: ${{ steps.detect.outputs.has-css }}
      package-manager: ${{ steps.detect-pm.outputs.manager }}
      install-cmd: ${{ steps.detect-pm.outputs.install-cmd }}
      is-turborepo: ${{ steps.detect-pm.outputs.is-turborepo }}
      turbo-prefix: ${{ steps.detect-pm.outputs.turbo-prefix }}
      quality-only: ${{ steps.change-scope.outputs.quality-only }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Detect change scope
        id: change-scope
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          QUALITY_ONLY=false
          if [ "${{ github.repository }}" != 'buildproven/qa-architect' ] &&
             { [ "${{ github.event_name }}" = 'pull_request' ] || [ "${{ github.event_name }}" = 'push' ]; } &&
             [ -n "$BASE_SHA" ] && git cat-file -e "$BASE_SHA^{commit}" 2>/dev/null; then
            APPLICATION_CHANGES="$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" | grep -vE '^(\.github/workflows/quality\.yml|\.buildproven/test-impact\.json)$' || true)"
            [ -n "$APPLICATION_CHANGES" ] || QUALITY_ONLY=true
          fi
          echo "quality-only=$QUALITY_ONLY" >> "$GITHUB_OUTPUT"

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Detect Package Manager
        id: detect-pm
        run: |
          # Detect package manager from lockfiles
          if [ -f pnpm-lock.yaml ]; then
            echo "manager=pnpm" >> $GITHUB_OUTPUT
            echo "install-cmd=pnpm install --frozen-lockfile" >> $GITHUB_OUTPUT
          elif [ -f yarn.lock ]; then
            echo "manager=yarn" >> $GITHUB_OUTPUT
            echo "install-cmd=yarn install --frozen-lockfile" >> $GITHUB_OUTPUT
          elif [ -f bun.lock ] || [ -f bun.lockb ]; then
            echo "manager=bun" >> $GITHUB_OUTPUT
            echo "install-cmd=bun install --frozen-lockfile" >> $GITHUB_OUTPUT
          elif [ -f package-lock.json ]; then
            echo "manager=npm" >> $GITHUB_OUTPUT
            echo "install-cmd=npm ci" >> $GITHUB_OUTPUT
          else
            echo "manager=npm" >> $GITHUB_OUTPUT
            echo "install-cmd=npm install" >> $GITHUB_OUTPUT
          fi

          # Detect Turborepo
          if [ -f turbo.json ]; then
            echo "is-turborepo=true" >> $GITHUB_OUTPUT
            echo "turbo-prefix=turbo run" >> $GITHUB_OUTPUT
            echo "📦 Turborepo detected - will use 'turbo run' for tasks"
          else
            echo "is-turborepo=false" >> $GITHUB_OUTPUT
            echo "turbo-prefix=" >> $GITHUB_OUTPUT
          fi

      - name: Setup pnpm
        if: steps.detect-pm.outputs.manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: '8.15.0'

      - name: Setup Bun
        if: steps.detect-pm.outputs.manager == 'bun'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.0.0'

      # {{FULL_DETECTION_BEGIN}}
      - name: Detect Project Maturity
        id: detect
        run: |
          # Use the project maturity detector (local for qa-architect itself, npx for consumer projects)
          if [ -f lib/project-maturity.js ]; then
            node lib/project-maturity.js --github-actions >> $GITHUB_OUTPUT
          else
            SOURCE_COUNT=$(find . -type f \( -name '*.js' -o -name '*.jsx' -o -name '*.ts' -o -name '*.tsx' -o -name '*.py' -o -name '*.sh' \) \
              -not -path './.git/*' -not -path './node_modules/*' -not -path './dist/*' -not -path './build/*' -not -path './coverage/*' | wc -l | tr -d ' ')
            TEST_COUNT=$(find . -type f \( -path '*/test/*' -o -path '*/tests/*' -o -path '*/__tests__/*' -o -name '*.test.js' -o -name '*.test.ts' -o -name '*.test.tsx' -o -name '*.spec.js' -o -name '*.spec.ts' -o -name '*.spec.tsx' \) \
              -not -path './.git/*' -not -path './node_modules/*' | wc -l | tr -d ' ')
            [ -f package.json ] && HAS_DEPS=true || HAS_DEPS=false
            [ -f README.md ] && HAS_DOCS=true || HAS_DOCS=false
            if find . -type f \( -name '*.css' -o -name '*.scss' -o -name '*.sass' -o -name '*.less' \) \
              -not -path './node_modules/*' -print -quit | grep -q .; then HAS_CSS=true; else HAS_CSS=false; fi
            if [ "$SOURCE_COUNT" -ge 20 ] && [ "$TEST_COUNT" -gt 0 ]; then MATURITY=production-ready
            elif [ "$SOURCE_COUNT" -ge 5 ]; then MATURITY=growing
            else MATURITY=minimal; fi
            {
              echo "maturity=$MATURITY"
              echo "source-count=$SOURCE_COUNT"
              echo "test-count=$TEST_COUNT"
              echo "has-deps=$HAS_DEPS"
              echo "has-docs=$HAS_DOCS"
              echo "has-css=$HAS_CSS"
            } >> "$GITHUB_OUTPUT"
          fi
      # {{FULL_DETECTION_END}}
      # {{FULL_REPORT_BEGIN}}
      - name: Display Detection Report
        run: |
          echo "📊 Project Detection Results"
          echo "Package Manager: ${{ steps.detect-pm.outputs.manager }}"
          echo "Install Command: ${{ steps.detect-pm.outputs.install-cmd }}"
          echo "Turborepo: ${{ steps.detect-pm.outputs.is-turborepo }}"
          echo "Maturity: ${{ steps.detect.outputs.maturity }}"
          echo "Source files: ${{ steps.detect.outputs.source-count }}"
          echo "Test files: ${{ steps.detect.outputs.test-count }}"
          echo "Has dependencies: ${{ steps.detect.outputs.has-deps }}"
          echo "Has documentation: ${{ steps.detect.outputs.has-docs }}"
          echo "Has CSS files: ${{ steps.detect.outputs.has-css }}"
      # {{FULL_REPORT_END}}

  # Note: Lint/format jobs REMOVED - pre-commit already does this locally
  # This follows industry best practice: "Each layer does unique work"
  # Pre-commit = lint + format on staged files
  # CI = affected tests + E2E + security (things that can't be done locally)

  # Step 2: Security checks - run if dependencies exist
  #
  # Optional Enhanced Token Configuration:
  # - GITLEAKS_TOKEN: Enhanced GitHub token for gitleaks (fallback: GITHUB_TOKEN)
  # - GITLEAKS_LICENSE: Commercial gitleaks license key for advanced features
  # - SEMGREP_APP_TOKEN: Semgrep app token for enhanced scanning and rate limits
  #
  # These are optional - workflow functions with defaults but enhanced tokens
  # provide better reliability on GHES/self-hosted and unlock premium features
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: detect-maturity
    # Least privilege: this job only needs to read the code and post a PR
    # comment. Scoping the token here means a compromised scan dependency
    # cannot push commits, alter releases, or touch other repo resources.
    permissions:
      contents: read
      pull-requests: write
    # Security MUST run at PR time, not only monthly — "CI green" has to mean
    # "secret-scanned + SAST-clean". Runs on PRs, direct pushes, the monthly
    # scheduled deep scan, and manual dispatch.
    if: >-
      (github.event_name == 'pull_request' || github.event_name == 'push' ||
       github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') &&
      needs.detect-maturity.outputs.has-deps == 'true' &&
      needs.detect-maturity.outputs.quality-only != 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: ${{ needs.detect-maturity.outputs.package-manager }}

      - name: Setup pnpm
        if: needs.detect-maturity.outputs.package-manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: '8.15.0'

      - name: Setup Bun
        if: needs.detect-maturity.outputs.package-manager == 'bun'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.0.0'

      - name: Install dependencies
        run: ${{ needs.detect-maturity.outputs.install-cmd }}

      - name: Verify dependency integrity
        run: |
          echo "🔐 Verifying dependency integrity..."
          PACKAGE_MANAGER="${{ needs.detect-maturity.outputs.package-manager }}"

          case "$PACKAGE_MANAGER" in
            "pnpm")
              if [ -f pnpm-lock.yaml ]; then
                pnpm install --frozen-lockfile --dry-run
                echo "✅ Dependency integrity verified (pnpm)"
              else
                echo "⚠️ No pnpm-lock.yaml found - skipping integrity verification"
              fi
              ;;
            "yarn")
              if [ -f yarn.lock ]; then
                yarn install --frozen-lockfile --dry-run
                echo "✅ Dependency integrity verified (yarn)"
              else
                echo "⚠️ No yarn.lock found - skipping integrity verification"
              fi
              ;;
            "bun")
              if [ -f bun.lockb ]; then
                bun install --frozen-lockfile --dry-run
                echo "✅ Dependency integrity verified (bun)"
              else
                echo "⚠️ No bun.lockb found - skipping integrity verification"
              fi
              ;;
            "npm"|*)
              if [ -f package-lock.json ]; then
                npm ci --dry-run --prefer-offline
                echo "✅ Dependency integrity verified (npm)"
              else
                echo "⚠️ No package-lock.json found - skipping integrity verification"
              fi
              ;;
          esac

      # ONE blocking dependency-CVE gate. Prod deps only: this tool ships via npx,
      # so production deps are what consumers download — that's the real "CI green
      # = clean" line. Dev-tooling CVEs don't reach users and are surfaced
      # (non-blocking) by the audit-report job instead. (Replaces the previous
      # three cascading audits — moderate-all, high-all, high-prod.)
      - name: Production dependency CVE gate
        run: |
          PACKAGE_MANAGER="${{ needs.detect-maturity.outputs.package-manager }}"
          echo "🔒 Auditing production dependencies (high+) with $PACKAGE_MANAGER..."
          case "$PACKAGE_MANAGER" in
            "pnpm") pnpm audit --audit-level high --prod ;;
            "yarn") yarn audit --level high --groups dependencies ;;
            "bun") bun audit --audit-level high --production ;;
            "npm"|*) npm audit --audit-level high --omit=dev ;;
          esac

      - name: Check for hardcoded secrets
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          GITLEAKS_VERSION='8.28.0'
          GITLEAKS_SHA256='5fd1b3b0073269484d40078662e921d07427340ab9e6ed526ccd215a565b3298'
          GITLEAKS_DIR="$(mktemp -d)"
          curl --fail --location --proto '=https' --tlsv1.2 \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            --output "${GITLEAKS_DIR}/gitleaks.tar.gz"
          tar -xzf "${GITLEAKS_DIR}/gitleaks.tar.gz" -C "${GITLEAKS_DIR}" gitleaks
          echo "${GITLEAKS_SHA256}  ${GITLEAKS_DIR}/gitleaks" | sha256sum --check -
          if [ "${{ github.event_name }}" = 'pull_request' ] || [ "${{ github.event_name }}" = 'push' ]; then
            "${GITLEAKS_DIR}/gitleaks" git --redact --no-banner --log-opts="$BASE_SHA..$HEAD_SHA" .
          else
            "${GITLEAKS_DIR}/gitleaks" git --redact --no-banner .
          fi

      - name: Security pattern detection
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          SEMGREP_ARGS=()
          if [ "${{ github.event_name }}" = 'pull_request' ] || [ "${{ github.event_name }}" = 'push' ]; then
            CHANGED_FILES="$(git diff --name-only "$BASE_SHA" "$HEAD_SHA")"
            APPLICATION_CHANGES="$(printf '%s\n' "$CHANGED_FILES" | grep -vE '^(\.github/workflows/quality\.yml|\.buildproven/test-impact\.json)$' || true)"
            if [ -z "$APPLICATION_CHANGES" ]; then
              echo 'Only generated quality policy files changed; no application security surface changed.'
              exit 0
            fi
            SEMGREP_ARGS+=(--baseline-commit "$BASE_SHA")
          fi
          docker run --rm \
            --mount "type=bind,source=$PWD,target=/src,readonly" \
            --workdir /src \
            --tmpfs /tmp:rw,nosuid,nodev,size=512m \
            --env HOME=/tmp \
            'semgrep/semgrep@sha256:65dcd4408adda7c183a6b4550cb1e9b19f7f627a6fbb7e0559bd466bedc44d7b' \
            semgrep scan \
            --config p/security-audit \
            --config p/javascript \
            --error \
            --metrics=off \
            "${SEMGREP_ARGS[@]}"

  # {{PRO_ASSURANCE_BEGIN}}
  # Pro changed-code gate. Configure QAA_LICENSE_JSON as a repository secret,
  # then require the `pr-assurance` check in branch protection.
  pr-assurance:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout exact pull request head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          # Fetch all refs before removing credentials so an advanced private-repo
          # base SHA remains resolvable without exposing a token to scanned code.
          fetch-depth: 0
          persist-credentials: false

      - name: Configure QA Architect entitlement
        if: github.repository != 'buildproven/qa-architect'
        env:
          QAA_LICENSE_JSON: ${{ secrets.QAA_LICENSE_JSON }}
          QAA_LICENSE_DIR: ${{ runner.temp }}/qa-architect-license
        run: |
          if [ -z "$QAA_LICENSE_JSON" ]; then
            echo "::error::QAA_LICENSE_JSON is required for the Pro PR assurance gate"
            exit 1
          fi
          install -d -m 700 "$QAA_LICENSE_DIR"
          printf '%s' "$QAA_LICENSE_JSON" > "$QAA_LICENSE_DIR/license.json"
          chmod 600 "$QAA_LICENSE_DIR/license.json"

      - name: Install pinned Semgrep engine
        env:
          PIPX_HOME: ${{ runner.temp }}/pipx
          PIPX_BIN_DIR: ${{ runner.temp }}/pipx-bin
        run: |
          pipx install 'semgrep==1.85.0'
          pipx runpip semgrep install --force-reinstall 'setuptools==80.9.0'
          "$PIPX_BIN_DIR/semgrep" --version
          echo "$PIPX_BIN_DIR" >> "$GITHUB_PATH"

      # {{QA_ARCHITECT_ONLY_BEGIN}}
      - name: Install QA Architect dependencies
        run: npm ci --ignore-scripts

      - name: Evaluate QA Architect's own changed-code assurance
        id: assurance_own
        continue-on-error: true
        env:
          NODE_ENV: test
          QAA_DEVELOPER: 'true'
          PIPX_BIN_DIR: ${{ runner.temp }}/pipx-bin
        run: |
          export PATH="$PIPX_BIN_DIR:$PATH"
          semgrep --version
          node setup.js --pr-check \
            --base-sha '${{ github.event.pull_request.base.sha }}' \
            --head '${{ github.event.pull_request.head.sha }}' \
            --artifact-dir '${{ runner.temp }}/qa-architect-assurance'
      # {{QA_ARCHITECT_ONLY_END}}
      - name: Evaluate changed-code assurance
        id: assurance_consumer
        if: github.repository != 'buildproven/qa-architect'
        continue-on-error: true
        env:
          QAA_LICENSE_DIR: ${{ runner.temp }}/qa-architect-license
          PIPX_BIN_DIR: ${{ runner.temp }}/pipx-bin
        run: |
          export PATH="$PIPX_BIN_DIR:$PATH"
          semgrep --version
          npx create-qa-architect@5.16.2 --pr-check \
            --base-sha '${{ github.event.pull_request.base.sha }}' \
            --head '${{ github.event.pull_request.head.sha }}' \
            --artifact-dir '${{ runner.temp }}/qa-architect-assurance'

      - name: Detect assurance evidence
        id: evidence
        if: always()
        env:
          EVIDENCE_DIR: ${{ runner.temp }}/qa-architect-assurance
          EXPECTED_BASE: ${{ github.event.pull_request.base.sha }}
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          for file in assurance.json assurance.sarif summary.md annotations.json manifest.json; do
            test -s "$EVIDENCE_DIR/$file"
          done
          node <<'NODE'
          const fs = require('fs')
          const path = require('path')
          const read = name => JSON.parse(fs.readFileSync(path.join(process.env.EVIDENCE_DIR, name), 'utf8'))
          const manifest = read('manifest.json')
          const assurance = read('assurance.json').assurance
          const sarif = read('assurance.sarif')
          read('annotations.json')
          const sarifRun = sarif.runs && sarif.runs[0]
          if (
            manifest.baseSha !== process.env.EXPECTED_BASE ||
            manifest.headSha !== process.env.EXPECTED_HEAD ||
            assurance.revision.value !== process.env.EXPECTED_HEAD ||
            manifest.verdict !== assurance.verdict ||
            !sarifRun ||
            sarifRun.automationDetails?.id !== `git-commit/${process.env.EXPECTED_HEAD}` ||
            sarifRun.properties?.assuranceVerdict !== assurance.verdict
          ) {
            throw new Error('Assurance evidence is incomplete or revision-inconsistent')
          }
          NODE
          verdict="$(node -p "require('$EVIDENCE_DIR/assurance.json').assurance.verdict")"
          case "$verdict" in PASS|BLOCK|INCOMPLETE) ;; *) exit 1 ;; esac
          echo "verdict=$verdict" >> "$GITHUB_OUTPUT"
          echo 'available=true' >> "$GITHUB_OUTPUT"

      - name: Publish assurance summary
        if: always() && steps.evidence.outputs.available == 'true'
        run: cat '${{ runner.temp }}/qa-architect-assurance/summary.md' >> "$GITHUB_STEP_SUMMARY"

      - name: Upload SARIF annotations
        if: always() && steps.evidence.outputs.available == 'true' && github.event.pull_request.head.repo.full_name == github.repository
        uses: github/codeql-action/upload-sarif@9e3211c9a3b9311dfe05da2ed48eea3386f042dd # v4.37.6
        with:
          sarif_file: ${{ runner.temp }}/qa-architect-assurance/assurance.sarif

      - name: Upload revision-bound evidence
        if: always() && steps.evidence.outputs.available == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: qa-architect-assurance-${{ github.event.pull_request.head.sha }}
          path: ${{ runner.temp }}/qa-architect-assurance
          if-no-files-found: error
          retention-days: 30

      - name: Enforce assurance verdict
        if: always()
        env:
          ASSURANCE_OUTCOME: ${{ steps.assurance_own.outcome || steps.assurance_consumer.outcome }}
          EVIDENCE_AVAILABLE: ${{ steps.evidence.outputs.available }}
          EVIDENCE_VERDICT: ${{ steps.evidence.outputs.verdict }}
        run: |
          if [ "$ASSURANCE_OUTCOME" != 'success' ] || [ "$EVIDENCE_AVAILABLE" != 'true' ] || [ "$EVIDENCE_VERDICT" != 'PASS' ]; then
            echo 'PR assurance did not produce a PASS verdict and complete evidence for this exact head.'
            exit 1
          fi
  # {{PRO_ASSURANCE_END}}
  # Step 2b: Advisory audit report (NON-BLOCKING). Runs in parallel with the
  # security gate (needs detect-maturity, NOT security) so a gate failure can
  # never suppress this report. Surfaces qa-architect's own vibe-code audit (the
  # AI-native rules generic SAST misses) plus a full-dep-tree advisory audit, and
  # posts them as a sticky PR comment. PR-only: the comment is meaningless on
  # push/schedule, so this adds no cost to those runs.
  audit-report:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: detect-maturity
    permissions:
      contents: read
      pull-requests: write
    if: github.event_name == 'pull_request' && needs.detect-maturity.outputs.has-deps == 'true' && needs.detect-maturity.outputs.quality-only != 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: ${{ needs.detect-maturity.outputs.package-manager }}

      - name: Setup pnpm
        if: needs.detect-maturity.outputs.package-manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: '8.15.0'

      - name: Setup Bun
        if: needs.detect-maturity.outputs.package-manager == 'bun'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.0.0'

      - name: Install dependencies
        run: ${{ needs.detect-maturity.outputs.install-cmd }}

      - name: Advisory full-tree dependency audit (non-blocking)
        run: |
          PACKAGE_MANAGER="${{ needs.detect-maturity.outputs.package-manager }}"
          echo "ℹ️ Advisory audit — all deps incl. dev tooling (does not block)" >> "$GITHUB_STEP_SUMMARY"
          case "$PACKAGE_MANAGER" in
            "pnpm") pnpm audit --audit-level=moderate >> "$GITHUB_STEP_SUMMARY" 2>&1 || true ;;
            "yarn") yarn audit --level=moderate >> "$GITHUB_STEP_SUMMARY" 2>&1 || true ;;
            "bun") bun audit --audit-level=moderate >> "$GITHUB_STEP_SUMMARY" 2>&1 || true ;;
            "npm"|*) npm audit --audit-level=moderate >> "$GITHUB_STEP_SUMMARY" 2>&1 || true ;;
          esac

      - name: Install semgrep CLI for vibe-audit
        # Install semgrep, then resolve where the binary actually landed and put
        # THAT dir on $GITHUB_PATH for the next step. pipx's bin dir varies by
        # runner (ubuntu-latest preconfigures PIPX_BIN_DIR=/opt/pipx_bin, not
        # ~/.local/bin), so we don't guess — we ask pipx for its bin dir and
        # verify `semgrep --version` resolves before continuing. Falls back to
        # `pip install --user` (~/.local/bin) if pipx is unavailable.
        # Best-effort: this is the ADVISORY job. If semgrep won't install, the
        # vibe-audit degrades to a "SCAN SKIPPED" report (via --no-fail) and the
        # job still passes — we never block a PR on advisory tooling.
        continue-on-error: true
        run: |
          set -e
          # Install semgrep into a dedicated venv with setuptools<81 PINNED.
          # Root cause of the recurring crash: semgrep (through 1.96) imports
          # pkg_resources, which setuptools >=81 no longer ships AND Python 3.12
          # no longer bundles — so on the 3.12 runner the import fails. Pinning
          # setuptools<81 restores pkg_resources. The Docker registry-rules action
          # in the security gate keeps 1.85.0 (its own runtime); only this CLI
          # needs the venv. (Local dev on py3.11 still bundles pkg_resources, which
          # masked this — hence verifying --version loudly on the 3.12 runner.)
          SEMGREP_CLI_VERSION=1.96.0
          python3 -m venv "$HOME/.semgrep-venv"
          "$HOME/.semgrep-venv/bin/pip" install --quiet --upgrade pip
          "$HOME/.semgrep-venv/bin/pip" install --quiet "setuptools<81" "semgrep==$SEMGREP_CLI_VERSION"
          echo "$HOME/.semgrep-venv/bin" >> "$GITHUB_PATH"
          "$HOME/.semgrep-venv/bin/semgrep" --version

      - name: QA Architect vibe-code audit
        run: |
          echo "🔍 Running QA Architect vibe-code security audit..."
          if [ -f setup.js ]; then
            node setup.js --audit --out qa-audit-report.md --no-fail
          else
            echo "QA Architect audit is available locally; consumer CI does not execute mutable external CLI code."
          fi

      - name: Post audit report as PR comment
        if: always() && hashFiles('qa-audit-report.md') != ''
        uses: marocchino/sticky-pull-request-comment@331f8f5b4215f0445d3c07b4967662a32a2d3e31 # v2.9.0
        with:
          header: qa-architect-audit
          path: qa-audit-report.md

  # Step 3: Tests - run if test files exist (development+)
  # Smart skip: Draft PRs skip tests (saves CI costs during WIP)
  tests:
    runs-on: ubuntu-latest
    # Selector-policy migrations run the complete suite. Keep the normal
    # focused-test budget below, but give that rare fail-closed path room.
    timeout-minutes: 20
    needs: detect-maturity
    if: |
      fromJSON(needs.detect-maturity.outputs.test-count) > 0 &&
      needs.detect-maturity.outputs.quality-only != 'true' &&
      (github.event.pull_request.draft != true || github.event_name != 'pull_request')
    strategy:
      fail-fast: false
      matrix:
        node-version: [22]

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: ${{ needs.detect-maturity.outputs.package-manager }}

      - name: Setup pnpm
        if: needs.detect-maturity.outputs.package-manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: '8.15.0'

      - name: Setup Bun
        if: needs.detect-maturity.outputs.package-manager == 'bun'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.0.0'

      - name: Install dependencies
        run: |
          echo "⏱️ Performance Budget: Dependency installation must complete within 2 minutes"
          timeout 120 ${{ needs.detect-maturity.outputs.install-cmd }} || {
            echo "::error::Dependency installation exceeded 2-minute performance budget!"
            exit 1
          }

      # {{QA_ARCHITECT_ONLY_BEGIN}}
      # Install the semgrep CLI so tests/semgrep-rule-precision.test.js actually
      # runs (it spawns `semgrep` and SKIPS when the binary is absent — a
      # skipped precision test gives false confidence that the audit rules
      # still distinguish real vulns from benign code). Linux-only; on other
      # runners the test skips by design.
      - name: Install semgrep CLI (for rule-precision tests)
        if: runner.os == 'Linux'
        run: |
          python3 -m pip install --quiet --user semgrep
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      # {{QA_ARCHITECT_ONLY_END}}
      - name: Run affected tests
        if: hashFiles('.buildproven/test-impact.json') != '' && (github.event_name == 'pull_request' || github.event_name == 'push')
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          CLAUDE_KIT_SHA: 6209610058b95a3d3f2a9d1af7a10f9c69f0dd69
        run: |
          set -euo pipefail
          if [ -z "$BASE_SHA" ] || ! git cat-file -e "$BASE_SHA:.buildproven/test-impact.json" 2>/dev/null; then
            CHANGED_FILES="$(git diff --name-only "$BASE_SHA" "$HEAD_SHA")"
            APPLICATION_CHANGES="$(printf '%s\n' "$CHANGED_FILES" | grep -vE '^(\.github/workflows/quality\.yml|\.buildproven/test-impact\.json)$' || true)"
            if [ -z "$APPLICATION_CHANGES" ]; then
              echo "Only generated quality policy files changed. The release generator already validated the new policy."
              exit 0
            fi
            echo "Base test-impact policy is absent. Running the complete migration audit once."
            timeout 900 npm test
            exit 0
          fi

          POLICY_ROOT="$RUNNER_TEMP/test-impact-policy"
          KIT_ROOT="$RUNNER_TEMP/claude-kit"
          mkdir -p "$POLICY_ROOT/.buildproven" "$KIT_ROOT"
          git show "$BASE_SHA:.buildproven/test-impact.json" > "$POLICY_ROOT/.buildproven/test-impact.json"
          if ! cmp -s "$POLICY_ROOT/.buildproven/test-impact.json" .buildproven/test-impact.json; then
            CHANGED_FILES="$(git diff --name-only "$BASE_SHA" "$HEAD_SHA")"
            APPLICATION_CHANGES="$(printf '%s\n' "$CHANGED_FILES" | grep -vE '^(\.github/workflows/quality\.yml|\.buildproven/test-impact\.json)$' || true)"
            if [ -z "$APPLICATION_CHANGES" ]; then
              echo "Only generated quality policy files changed. The release generator already validated the policy."
              exit 0
            fi
            echo "Test-impact policy and application code changed together. Running the complete selector-policy audit."
            timeout 900 npm test
            exit 0
          fi
          git -C "$KIT_ROOT" init --quiet
          git -C "$KIT_ROOT" remote add origin https://github.com/buildproven/claude-kit.git
          git -C "$KIT_ROOT" fetch --quiet --filter=blob:none origin main
          git -C "$KIT_ROOT" merge-base --is-ancestor "$CLAUDE_KIT_SHA" FETCH_HEAD
          git -C "$KIT_ROOT" checkout --quiet --detach "$CLAUDE_KIT_SHA"
          test "$(git -C "$KIT_ROOT" rev-parse HEAD)" = "$CLAUDE_KIT_SHA"

          POLICY_SHA256="$(sha256sum "$POLICY_ROOT/.buildproven/test-impact.json" | awk '{print $1}')"
          timeout 300 node "$KIT_ROOT/scripts/test-impact.js" \
            --execute \
            --policy-root "$POLICY_ROOT" \
            --policy-sha256 "$POLICY_SHA256" \
            --git-range "$BASE_SHA" "$HEAD_SHA" \
            --
      - name: Run tests
        if: hashFiles('.buildproven/test-impact.json') == '' || (github.event_name != 'pull_request' && github.event_name != 'push')
        run: |
          echo "🧪 Running ${{ needs.detect-maturity.outputs.test-count }} test files on Node.js ${{ matrix.node-version }}..."

          # Warn if test count is suspiciously low
          TEST_COUNT=${{ needs.detect-maturity.outputs.test-count }}
          if [ "$TEST_COUNT" -lt 5 ]; then
            echo "::warning::Only $TEST_COUNT test file(s) found - consider adding more tests for better coverage"
          fi

          # Performance budget: Test suite timeout (5 minutes max)
          echo "⏱️ Performance Budget: Test suite must complete within 5 minutes"
          timeout 300 npm test || {
            echo "::error::Test suite exceeded 5-minute performance budget!"
            exit 1
          }

      # {{COMPREHENSIVE_MUTATION_GATE_BEGIN}}
      - name: Run project mutation confidence gate
        run: |
          echo "🧬 Running the project-provided mutation command when configured..."
          npm run --if-present test:mutation
      # {{COMPREHENSIVE_MUTATION_GATE_END}}
      # {{QA_ARCHITECT_ONLY_BEGIN}}
      - name: Cache gitleaks binary for real download test
        if: runner.os == 'Linux'
        uses: actions/cache@v5
        with:
          path: ~/.cache/create-qa-architect/gitleaks
          key: gitleaks-8.28.0-linux-x64-a65b5253-${{ hashFiles('lib/validation/config-security.js') }}
          restore-keys: |
            gitleaks-8.28.0-linux-x64-a65b5253-
            gitleaks-8.28.0-linux-x64-

      - name: Run real gitleaks binary verification test
        if: runner.os == 'Linux' && hashFiles('tests/gitleaks-real-binary-test.js') != ''
        run: |
          echo "🔐 Running real gitleaks binary verification test..."
          QAA_DEVELOPER=true RUN_REAL_BINARY_TEST=1 node tests/gitleaks-real-binary-test.js
      # {{QA_ARCHITECT_ONLY_END}}

  # Step 4: Documentation - run for production-ready projects
  documentation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: detect-maturity
    if: needs.detect-maturity.outputs.maturity == 'production-ready' && needs.detect-maturity.outputs.quality-only != 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: ${{ needs.detect-maturity.outputs.package-manager }}

      - name: Setup pnpm
        if: needs.detect-maturity.outputs.package-manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: '8.15.0'

      - name: Setup Bun
        if: needs.detect-maturity.outputs.package-manager == 'bun'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.0.0'

      - name: Install dependencies
        run: ${{ needs.detect-maturity.outputs.install-cmd }}

      # (Secret scanning lives in the `security` gate job, which runs on every
      # PR/push. It was duplicated here previously — removed to avoid running
      # gitleaks twice per workflow.)

      - name: Documentation validation
        run: |
          echo "📖 Running documentation validation..."
          if [ -f setup.js ]; then
            node setup.js --validate-docs
          else
            test -f README.md
          fi

      - name: Documentation consistency and security audit freshness
        continue-on-error: true
        run: |
          echo "🔐 Running comprehensive documentation validation..."
          # This includes security audit freshness check with proper git-based validation
          if [ -f scripts/check-docs.sh ]; then
            bash scripts/check-docs.sh
          else
            echo "No check-docs.sh found - skipping documentation consistency check"
          fi

      - name: Package size and contents validation
        if: hashFiles('package.json') != ''
        run: |
          echo "📦 Validating package size and contents..."

          # Check if this is an npm package
          if [ -f package.json ] && grep -q '"name"' package.json; then
            # Dry-run pack to check what would be included
            echo "🔍 Checking package contents (dry run):"
            npm pack --dry-run 2>/dev/null | grep -E '^[a-zA-Z]' || echo "No files listed"

            # Calculate estimated pack size
            PACK_SIZE=$(npm pack --dry-run 2>&1 | grep -E 'package size.*[0-9]+' | grep -oE '[0-9.]+\s?(B|kB|MB)' | head -1 || echo "unknown")
            echo "📊 Estimated package size: $PACK_SIZE"

            # Warn if package seems too large (>10MB)
            if echo "$PACK_SIZE" | grep -qE '[0-9]+\s?MB'; then
              SIZE_NUM=$(echo "$PACK_SIZE" | grep -oE '[0-9]+')
              if [ "$SIZE_NUM" -gt 10 ]; then
                echo "::warning::Package size ($PACK_SIZE) is quite large. Consider excluding unnecessary files via .npmignore or package.json 'files' field."
              fi
            fi

            # Check for common files that shouldn't be in packages
            echo "🔍 Checking for potentially unwanted files..."
            npm pack --dry-run 2>/dev/null | grep -E '\.(log|tmp|cache|DS_Store)$|node_modules/|\.git/' && {
              echo "::warning::Package contains files that might not belong in the published package"
            } || echo "✅ No obviously unwanted files detected"

            # Validate package.json files field if present
            if grep -q '"files"' package.json; then
              echo "✅ Package.json 'files' field configured"
            else
              echo "::notice::No 'files' field in package.json - all files except those in .npmignore will be included"
            fi
          else
            echo "ℹ️ Not an npm package or no package.json found - skipping package validation"
          fi

      - name: Detect web app
        id: detect-webapp
        run: |
          # Only run Lighthouse for actual web apps, not CLI tools
          IS_WEBAPP=false

          # Check for web app indicators
          if [ -f "index.html" ] || [ -f "public/index.html" ] || [ -f "src/index.html" ]; then
            IS_WEBAPP=true
          elif [ -d "dist" ] && [ -f "dist/index.html" ]; then
            IS_WEBAPP=true
          elif grep -q '"start".*"next\|vite\|react-scripts\|serve"' package.json 2>/dev/null; then
            IS_WEBAPP=true
          fi

          echo "is-webapp=$IS_WEBAPP" >> $GITHUB_OUTPUT
          echo "Web app detected: $IS_WEBAPP"

      - name: Lighthouse CI
        if: hashFiles('.lighthouserc.cjs', '.lighthouserc.js', '.lighthouserc.json', 'lighthouserc.js') != '' && steps.detect-webapp.outputs.is-webapp == 'true'
        id: lighthouse
        run: |
          echo "Running Lighthouse CI..."
          npx lhci autorun
        continue-on-error: true

      - name: Report Lighthouse Failures
        if: steps.lighthouse.outcome == 'failure'
        run: |
          echo "::warning::Lighthouse CI failed - performance budgets or quality thresholds violated"
          echo "Review the Lighthouse report to see which metrics failed."
          echo "Common failures: performance score, accessibility issues, SEO problems"

          # Add to job summary for visibility
          echo "## Lighthouse CI Failed" >> $GITHUB_STEP_SUMMARY
          echo "Performance budgets or quality thresholds were violated." >> $GITHUB_STEP_SUMMARY
          echo "Review the Lighthouse report in the Actions logs above." >> $GITHUB_STEP_SUMMARY

          # Soft warning, not hard fail - Lighthouse issues shouldn't block releases
          # Teams can enforce by adding 'error' level assertions in their lighthouserc
          echo "::notice::Lighthouse failures are warnings. Add 'error' assertions in .lighthouserc.js to enforce."

  # Step 5: Summary - report what checks ran
  summary:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - detect-maturity
      - security
      - audit-report
      - tests
      - documentation
    if: always()

    steps:
      - name: Generate Check Summary
        env:
          SECURITY_RESULT: ${{ needs.security.result }}
          TESTS_RESULT: ${{ needs.tests.result }}
          DOCS_RESULT: ${{ needs.documentation.result }}
        run: |
          echo "## Quality Checks Summary 📊" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Maturity Level:** ${{ needs.detect-maturity.outputs.maturity }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Project Statistics" >> $GITHUB_STEP_SUMMARY
          echo "- Source files: ${{ needs.detect-maturity.outputs.source-count }}" >> $GITHUB_STEP_SUMMARY
          echo "- Test files: ${{ needs.detect-maturity.outputs.test-count }}" >> $GITHUB_STEP_SUMMARY
          echo "- Has dependencies: ${{ needs.detect-maturity.outputs.has-deps }}" >> $GITHUB_STEP_SUMMARY
          echo "- Has documentation: ${{ needs.detect-maturity.outputs.has-docs }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Checks Executed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Note: Lint/format checks run locally in pre-commit (not duplicated here)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Security
          if [ "$SECURITY_RESULT" == "success" ]; then
            echo "- ✅ Security: Passed" >> $GITHUB_STEP_SUMMARY
          elif [ "$SECURITY_RESULT" == "failure" ]; then
            echo "- ❌ Security: Failed" >> $GITHUB_STEP_SUMMARY
          elif [ "$SECURITY_RESULT" == "skipped" ]; then
            echo "- ⏭️ Security: Skipped (runs on schedule/manual trigger)" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ⚠️ Security: $SECURITY_RESULT" >> $GITHUB_STEP_SUMMARY
          fi

          # Tests
          if [ "$TESTS_RESULT" == "success" ]; then
            echo "- ✅ Tests: Passed" >> $GITHUB_STEP_SUMMARY
          elif [ "$TESTS_RESULT" == "failure" ]; then
            echo "- ❌ Tests: Failed" >> $GITHUB_STEP_SUMMARY
          elif [ "$TESTS_RESULT" == "skipped" ]; then
            echo "- ⏭️ Tests: Skipped (no test files)" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ⚠️ Tests: $TESTS_RESULT" >> $GITHUB_STEP_SUMMARY
          fi

          # Documentation
          if [ "$DOCS_RESULT" == "success" ]; then
            echo "- ✅ Documentation: Passed" >> $GITHUB_STEP_SUMMARY
          elif [ "$DOCS_RESULT" == "failure" ]; then
            echo "- ❌ Documentation: Failed" >> $GITHUB_STEP_SUMMARY
          elif [ "$DOCS_RESULT" == "skipped" ]; then
            echo "- ⏭️ Documentation: Skipped (not production-ready)" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ⚠️ Documentation: $DOCS_RESULT" >> $GITHUB_STEP_SUMMARY
          fi
            # PR comment step not enabled (use --pr-comments to add)

      # {{QA_ARCHITECT_ONLY_BEGIN}}
      # This job is the required status check in branch protection, so it must
      # actually gate. Without this step it runs with `if: always()` and exits
      # 0 unconditionally — a failing security or test job would still report a
      # green required check and merge cleanly.
      #
      # `skipped` is a pass: jobs are deliberately conditional (path filters,
      # no test files, docs only on production-ready). `cancelled` is a fail —
      # it must not read as success.
      - name: Enforce check results
        env:
          DETECT_RESULT: ${{ needs.detect-maturity.result }}
          SECURITY_RESULT: ${{ needs.security.result }}
          AUDIT_RESULT: ${{ needs.audit-report.result }}
          TESTS_RESULT: ${{ needs.tests.result }}
          DOCS_RESULT: ${{ needs.documentation.result }}
        run: |
          failed=""
          for check in \
            "detect-maturity:$DETECT_RESULT" \
            "security:$SECURITY_RESULT" \
            "audit-report:$AUDIT_RESULT" \
            "tests:$TESTS_RESULT" \
            "documentation:$DOCS_RESULT"
          do
            name="${check%%:*}"
            result="${check#*:}"
            case "$result" in
              success|skipped) ;;
              *) failed="$failed $name($result)" ;;
            esac
          done

          if [ -n "$failed" ]; then
            echo "❌ Quality gate failed:$failed"
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**❌ Quality gate failed:**$failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "✅ Quality gate passed"
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**✅ Quality gate passed**" >> $GITHUB_STEP_SUMMARY
      # {{QA_ARCHITECT_ONLY_END}}

  # Slack alerts not enabled (use --alerts-slack to add)
